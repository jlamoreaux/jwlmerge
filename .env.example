# JWL Merge runs entirely in the browser: backups are never uploaded, and the
# app needs no database, storage or API keys to build and run.
#
# This file is kept as a placeholder. Add any future client-side configuration
# here, prefixed with NEXT_PUBLIC_ so it reaches the browser.
